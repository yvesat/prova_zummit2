name: prova_zummit
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flex_color_scheme: ^6.1.2
  font_awesome_flutter: ^10.4.0
  intl: ^0.18.0
  hooks_riverpod: ^2.2.0
  isar: ^3.0.5
  money_formatter: ^0.0.4
  http: ^0.13.5
  currency_text_input_formatter: ^2.1.8

dependency_overrides:
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/instituicoes.json
    - assets/convenios.json
    - assets/simulacao.json
